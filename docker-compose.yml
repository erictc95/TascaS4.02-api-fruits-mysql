services:

  mysql:
    image: mysql:8.4

    container_name: fruit-mysql

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: fruitdb

    ports:
      - "3310:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: